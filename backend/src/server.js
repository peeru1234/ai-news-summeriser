import 'dotenv/config';
import express from 'express'; import cors from 'cors'; import jwt from 'jsonwebtoken'; import bcrypt from 'bcryptjs'; import {z} from 'zod'; import {GoogleGenerativeAI} from '@google/generative-ai'; import {db,id} from './store.js';
const app=express(), secret=process.env.JWT_SECRET||'development-secret-change-me';
app.use(cors({origin:[process.env.FRONTEND_URL,'http://localhost:5173'].filter(Boolean)})); app.use(express.json({limit:'1mb'}));
const auth=(req,res,next)=>{try{req.user=jwt.verify(req.headers.authorization?.replace('Bearer ',''),secret);next()}catch{res.status(401).json({error:'Authentication required'})}};
const safe=u=>({id:u.id,name:u.name,email:u.email,role:u.role,createdAt:u.createdAt});
const owned=(req,res,next)=>{const c=db.cases.find(x=>x.id===req.params.id&&x.userId===req.user.id);if(!c)return res.status(404).json({error:'Verification case not found'});req.case=c;next()};
app.get('/api/health',(_,res)=>res.json({ok:true}));
app.post('/api/auth/register',async(req,res,next)=>{try{const b=z.object({name:z.string().min(2),email:z.string().email(),password:z.string().min(8)}).parse(req.body);if(db.users.some(u=>u.email===b.email))return res.status(409).json({error:'Email already registered'});const u={id:id(),...b,password:await bcrypt.hash(b.password,12),role:'contributor',createdAt:new Date().toISOString()};db.users.push(u);res.status(201).json({token:jwt.sign({id:u.id},secret,{expiresIn:'7d'}),user:safe(u)})}catch(e){next(e)}});
app.post('/api/auth/login',async(req,res,next)=>{try{const b=z.object({email:z.string().email(),password:z.string().min(1)}).parse(req.body),u=db.users.find(x=>x.email===b.email);if(!u||!await bcrypt.compare(b.password,u.password))return res.status(401).json({error:'Invalid email or password'});res.json({token:jwt.sign({id:u.id},secret,{expiresIn:'7d'}),user:safe(u)})}catch(e){next(e)}});
app.get('/api/profile',auth,(req,res)=>res.json(safe(db.users.find(u=>u.id===req.user.id))));
app.put('/api/profile',auth,(req,res,next)=>{try{const b=z.object({name:z.string().min(2)}).parse(req.body),u=db.users.find(x=>x.id===req.user.id);u.name=b.name;res.json(safe(u))}catch(e){next(e)}});
app.get('/api/cases',auth,(req,res)=>{const q=(req.query.q||'').toLowerCase();res.json(db.cases.filter(c=>c.userId===req.user.id&&!c.archived).filter(c=>!q||JSON.stringify(c).toLowerCase().includes(q)).sort((a,b)=>b.updatedAt.localeCompare(a.updatedAt)))});
app.post('/api/cases',auth,(req,res,next)=>{try{const b=z.object({headline:z.string().min(4),articleText:z.string().min(10),sourceUrl:z.string().url().optional().or(z.literal('')),publication:z.string().optional(),publicationDate:z.string().optional(),priority:z.enum(['low','medium','high']).default('medium')}).parse(req.body),now=new Date().toISOString();const c={id:id(),userId:req.user.id,...b,status:'unreviewed',claims:[],evidence:[],analysis:null,archived:false,createdAt:now,updatedAt:now};db.cases.push(c);res.status(201).json(c)}catch(e){next(e)}});
app.get('/api/cases/:id',auth,owned,(req,res)=>res.json(req.case));
app.patch('/api/cases/:id',auth,owned,(req,res)=>{Object.assign(req.case,req.body,{updatedAt:new Date().toISOString()});res.json(req.case)});
app.delete('/api/cases/:id',auth,owned,(req,res)=>{req.case.archived=true;req.case.updatedAt=new Date().toISOString();res.status(204).end()});
app.post('/api/cases/:id/evidence',auth,owned,(req,res,next)=>{try{const e=z.object({position:z.enum(['supporting','contradicting','contextual']),note:z.string().min(3),source:z.string().min(2)}).parse(req.body);req.case.evidence.push({id:id(),...e,createdAt:new Date().toISOString()});req.case.updatedAt=new Date().toISOString();res.status(201).json(req.case)}catch(e){next(e)}});
const prompt=c=>`You are a careful fact-checking research assistant. Never declare a claim true or false. Analyze this article and return ONLY JSON with summary, uncertainty, claims (array of {text,type,priority}), loadedLanguage (array), questions (array), evidenceGaps (array), checklist (array). Article: ${c.articleText}`;
app.post('/api/cases/:id/analyze',auth,owned,async(req,res)=>{let a;try{if(!process.env.GEMINI_API_KEY)throw Error('GEMINI_API_KEY is not configured');const ai=new GoogleGenerativeAI(process.env.GEMINI_API_KEY);const out=await ai.getGenerativeModel({model:process.env.GEMINI_MODEL||'gemini-2.5-flash'}).generateContent(prompt(req.case));a=JSON.parse(out.response.text().replace(/```json|```/g,''))}catch(error){console.error('Gemini analysis failed:',error.message);a={summary:'Draft neutral overview: review the submitted article against independently sourced evidence before drawing conclusions.',uncertainty:'AI analysis is a research aid. Publication details, source provenance, dates, and quoted data require reviewer verification.',claims:[{text:req.case.headline,type:'reported assertion',priority:req.case.priority}],loadedLanguage:['Review for emotionally charged or absolute wording in the submitted text.'],questions:['Which primary source directly supports the central assertion?','Do independent sources report the same date, number, and scope?'],evidenceGaps:['No independently reviewed supporting evidence has been recorded yet.'],checklist:['Locate the original source or dataset','Verify dates, names, and numeric figures','Record supporting and contradicting sources','Reviewer approves any final conclusion']}}req.case.analysis=a;req.case.claims=a.claims;req.case.updatedAt=new Date().toISOString();res.json(req.case)});
app.use((err,req,res,next)=>{if(err instanceof z.ZodError)return res.status(400).json({error:err.issues[0].message});console.error(err);res.status(500).json({error:'Something went wrong'})});
app.get('/',(_,res)=>res.send('API is running successfully!'));
app.listen(process.env.PORT||5000,()=>console.log('Verity API running'));
